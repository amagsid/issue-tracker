'use client';
import React from 'react';

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => alert('added')}>Add to cart</button>
    </div>
  );
};

export default AddToCart;
